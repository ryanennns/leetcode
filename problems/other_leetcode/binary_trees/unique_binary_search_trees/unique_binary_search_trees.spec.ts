import { describe, expect, it } from "vitest";
import { numTrees } from "@/other_leetcode/binary_trees/unique_binary_search_trees/unique_binary_serach_trees.js";

describe("numTrees", () => {
  it("snickers", () => {
    expect(numTrees(5)).toBe(42);
  });
});
